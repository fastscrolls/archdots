# Query my default IP address only on startup
[volume-pipewire]
command=bash ./pipewire-volume
interval=persist
SUBSCRIBE=1
USE_ALSA_NAME=1

# Update time every 5 seconds
[time]
command=date +%T
interval=5
